<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="crabster.rudakov.sberschoolfinalproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name="crabster.rudakov.sberschoolfinalproject.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SberSchoolFinalProject"
        android:usesCleartextTraffic="true"
        tools:ignore="Instantiatable"
        tools:targetApi="m">
        <activity
            android:name="crabster.rudakov.sberschoolfinalproject.ui.splashScreen.SplashActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SberSchoolFinalProject.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />

        <activity
            android:name="crabster.rudakov.sberschoolfinalproject.ui.main.view.MainActivity"
            android:exported="true"
            tools:ignore="Instantiatable">
        </activity>
    </application>

</manifest>